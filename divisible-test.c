#include <stdio.h>


int main(){
    //program to check wheather if a number is divisible by 97 or not 
    printf("program to check wheather if a number is divisible by 97 or not. Enter the number \n");
    int a;
    scanf("%d",&a);
    if (a%97!=0)) {
        printf("Number not divisible by")
    }






    return 0
}